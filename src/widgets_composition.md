# Widgets Composition

Now that we know how to edit a simple widget, let us look into the use of widgets inside widgets.

### Understanding Widget Trees

#@todo, explain that widgets are composed of widgets and that there is a widget tree
#@todo, show with the dev tools what the current tree looks like

### Widget Wrapping Patterns

#@todo, explain that different widgets may wrap widgets differently (body, child, children)

### Practice

1. Duplicate your Movie Text using a `Row()` widget
2. Wrap each movie title in a `Container()` to highlight them better
3. Refactor to extract a new custom `FilmTitle()` widget
