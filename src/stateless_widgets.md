# Stateless Widgets
#@todo, explained widgets and mainly stateless
[text](https://api.flutter.dev/flutter/widgets/StatelessWidget-class.html#widgets.StatelessWidget.1)

## Practice

1. **Create a FilmTitle widget** to remove the duplicate code of `Container(Text())`:
   - Create a class `FilmTitle()` that extends `StatelessWidget`
   - Add a `final String film` property
   - In the `build()` method, return the previously defined `Container(Text())`
